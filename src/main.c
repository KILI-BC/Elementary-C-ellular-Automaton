#include <stdlib.h>
#include <stdio.h>

#define TAPE_WIDTH 32
typedef int tape;

tape calc_next(tape b);
void print_band(tape b);

int main()
{
	return EXIT_SUCCESS;
}

tape calc_next(tape b)
{

}

void print_band(tape b)
{

}